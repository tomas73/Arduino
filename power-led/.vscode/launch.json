{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Arduino Compile",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}",
      "preLaunchTask": "Arduino Compile",
      "console": "integratedTerminal",
      "stopOnEntry": false
    }
  ]
}
